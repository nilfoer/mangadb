# in root of your repository or in your tests folder
[pytest]
# adopts = -vv  <- always run with vv output
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    requires_cookies: requires cookies like Cloudflare challenge clearance cookies
